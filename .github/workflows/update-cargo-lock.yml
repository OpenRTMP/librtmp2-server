name: Update Cargo.lock

on:
  push:
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  update-cargo-lock:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - name: Check branch still exists
        id: preflight
        if: github.ref_type == 'branch'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh api "repos/${{ github.repository }}/git/ref/heads/${{ github.ref_name }}" >/dev/null 2>&1; then
            echo "skip=false" >> "$GITHUB_OUTPUT"
          else
            echo "Branch '${{ github.ref_name }}' was deleted before checkout; skipping Cargo.lock update."
            echo "skip=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout repository
        if: steps.preflight.outputs.skip != 'true'
        uses: actions/checkout@v7
        with:
          ref: ${{ github.ref }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Rust
        if: steps.preflight.outputs.skip != 'true'
        uses: dtolnay/rust-toolchain@stable

      - name: Update Cargo.lock
        if: steps.preflight.outputs.skip != 'true'
        run: cargo generate-lockfile

      - name: Commit updated Cargo.lock
        if: steps.preflight.outputs.skip != 'true'
        id: commit-lock
        run: |
          if git diff --quiet Cargo.lock; then
            echo "Cargo.lock is already up to date."
            echo "updated=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Cargo.lock
          git commit -m "Update Cargo.lock"
          git push
          echo "updated=true" >> "$GITHUB_OUTPUT"

      - name: Trigger downstream CI workflows
        if: steps.commit-lock.outputs.updated == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh workflow run tests.yml --ref "${{ github.ref_name }}"
          gh workflow run e2e-tests.yml --ref "${{ github.ref_name }}"
          gh workflow run benchmarks.yml --ref "${{ github.ref_name }}"
